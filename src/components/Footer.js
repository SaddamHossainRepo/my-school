import React, { useContext } from 'react';
import { TeacherContext } from './Main';


const Footer = () => {
    // const teachers = useContext(TeacherContext);
    
    return (
        <div className='bg bg-cyan-500 h-20 relative'>
            <h1 className=''>npm install react-slick --save</h1>
        </div>
    );
};

export default Footer; <h1>Footer</h1>